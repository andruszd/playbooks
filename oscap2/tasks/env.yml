---
# - name: Configure Enviroment Files
#   vars:
#      dahostname: "{{ hostvars[inventory_hostname] }}"

#   template:
#     src: "{{ item.src }}"
#     dest: "{{ item.dest }}"
#     owner: "{{ item.owner }}"
#     group: "{{ item.group }}"
#     mode: "{{ item.mode }}"
#   with_items:
#     - { src: "templates",
#         dest: "{{ _env_file }}",
#         owner: "{{ _user }}",
#         group: "{{ _group }}",
#         mode: "0644" }