monkeyble_scenarios:
  scenario_1:
    name: "Updates"
    tasks_to_test:
      - task: "update all packages"
        # Test the task with the following input
        test_input:
          - assert_equal:
              arg_name: msg
              expected: "{{ failed_shared_test }}"