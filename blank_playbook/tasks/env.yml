---
# - name: Configure Enviroment Files
#   vars:
#      dahostname: "{{ hostvars[inventory_hostname] }}"

#   template:
#     src: "{{ item.src }}"
#     dest: "{{ item.dest }}"
#     owner: "{{ item.owner }}"
#     group: "{{ item.group }}"
#     mode: "{{ item.mode }}"
#   with_items:
#     - { src: "templates",
#         dest: "{{ ot_controller_env_file }}",
#         owner: "{{ p_user }}",
#         group: "{{ _group }}",
#         mode: "0644" }